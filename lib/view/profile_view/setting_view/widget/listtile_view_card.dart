import 'package:flutter/material.dart';

class ListtileViewCard extends StatelessWidget {
  const ListtileViewCard({super.key});

  @override
  Widget build(BuildContext context) {
    return ListTile(

    );
  }
}
